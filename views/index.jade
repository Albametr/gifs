extends layout

block content
  - nbsp = '&nbsp'
  p!= nbsp
  .container.text-center
      h1= title
      .row(ng-repeat='item in gifs')
        .col-sm-10.col-sm-offset-1
            div(gf-gif url='{{item.videoUri}}')
          
  include ./angular/list.jade
